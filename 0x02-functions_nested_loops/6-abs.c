#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 *
 * @x : number to be computed
 * Return: 0
 */

int _abs(int x)
{
	if (x < 0)
	{
		x = (-1) * x;
	_putchar(x + '0');
	}

			return (0);
}
