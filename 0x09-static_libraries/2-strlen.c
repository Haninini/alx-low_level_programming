#include "main.h"

/**
 * _strlen - returns the length of the string
 * @s: variable that contains a char array to be counted
 * Return: length of the string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
